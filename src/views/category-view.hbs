{{> menu}}

<section class="section columns is-vcentered">
  <div class="column has-text-centered">
    <div class="title">
      {{category.name}}
    </div>
    {{> list-pois}}
    {{> add-poi}}
  </div>
</section>